package ExampeOfClassAnimal;
class Dog {
    String name; // Attribute
    int age;     // Attribute
    void bark() { // Behavior (Method)
        System.out.println(name + " is barking!");
    }
}
